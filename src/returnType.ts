// if define return type if other type happen this make error 

function addRetrun(n1: number, n2: number): number {
    return n1 + n2;
  }

//if dont define return we must write this 
 //const addwithAlias = (n1 : combine , n2 : combine)=>{
//     if(typeof n1 === "number" && typeof n2 === "number")
//     return n1 + n2 
//     return n1.toString() +  n2.toString()
// }